name: mynew ecr

on:
  push:
    branches:
      - main


jobs:
  build_and_push:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        
      - name: Dockerpull
        run: |
         sudo docker build -t mynginx . 
         sudo docker run --name yadavyadavvv -d -p 7778:80 mynginx
